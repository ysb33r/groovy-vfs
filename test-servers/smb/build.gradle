

dependencies {
    implementation project(path : ':jlan', configuration : 'jlan')// fileTree( dir : "${project(':jlan').buildDir}/libs", include: '*.jar' )
    implementation "org.codehaus.groovy:groovy-xml:${groovyVer}"

}

//classes.dependsOn project(':jlan').tasks.jar

